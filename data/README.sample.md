# data

This directory stores local sync cache data.

- WeCom smart-sheet sync output is stored under `data/wecom/`
- Feishu bitable sync output is stored under `data/feishu/`
- These are runtime cache files and should not be committed
